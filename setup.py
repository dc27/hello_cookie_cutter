from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='what are the haps with cookie cutter',
    author='dc27',
    license='MIT',
)
